<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Phonebook.MainPage"
             Title="Телефонный справочник">

    <Grid RowDefinitions="Auto, *, Auto" Padding="10">

        <!-- Поиск -->
        <SearchBar x:Name="searchBar"
                   Grid.Row="0"
                   Placeholder="Поиск контактов..."
                   TextChanged="SearchBar_TextChanged" />

        <!-- Список контактов -->
        <CollectionView x:Name="contactsList"
                        Grid.Row="1"
                        SelectionMode="Single"
                        SelectionChanged="OnContactSelected">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="5" Padding="10" CornerRadius="10">
                        <Grid ColumnDefinitions="60, *">
                            <Image Grid.Column="0"
                                   Source="{Binding PhotoPath, TargetNullValue='person.png', FallbackValue='person.png'}"
                                   HeightRequest="50"
                                   WidthRequest="50"
                                   Aspect="AspectFill" />

                            <VerticalStackLayout Grid.Column="1" Margin="10,0,0,0">
                                <Label Text="{Binding Name}"
                                       FontSize="18"
                                       FontAttributes="Bold" />
                                <Label Text="{Binding PhoneNumber}"
                                       FontSize="14"
                                       TextColor="Gray" />
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>

            <CollectionView.EmptyView>
                <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center">
                    <Label Text="Контакты не найдены" FontSize="18" TextColor="Gray" />
                </VerticalStackLayout>
            </CollectionView.EmptyView>
        </CollectionView>

        <!-- Кнопки внизу -->
        <HorizontalStackLayout Grid.Row="2" 
                               HorizontalOptions="Center" 
                               Spacing="20"
                               Margin="0,10,0,0">
            <Button Text="+ Добавить"
                    Clicked="OnAddContactClicked" />
            <Button Text="Экспорт"
                    Clicked="OnExportContactClicked" />
        </HorizontalStackLayout>
    </Grid>
</ContentPage>